public class Heritage {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Chocolat noir = new Chocolat("emballe de luxe", "chocolat noir");
        System.out.println(noir.getNom() + " - " + noir.getEmballage());
        // pdt.emballage;
    }
    
}
